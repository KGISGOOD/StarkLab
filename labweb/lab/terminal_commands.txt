# Run these commands in your terminal to start the Django server:

# Step 1: Activate the Anaconda environment
conda activate starklab_env

# Step 2: Navigate to your Django project directory (if you're not already there)
cd /Users/che/Desktop/StarkLab/labweb/lab

# Step 3: Run the Django server
python manage.py runserver
